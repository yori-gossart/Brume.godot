[gd_resource type="Resource" script_class="ItemDefinition" load_steps=2 format=3]

; BOIS — the benchmark's test resource, now described as data instead of an
; enum branch. Heavy enough that carrying a lot of it will matter once the
; 0.3 weight system exists.

[ext_resource type="Script" path="res://scripts/data/item_definition.gd" id="1_item"]

[resource]
script = ExtResource("1_item")
id = &"BOIS"
display_name = "Bois"
category = 0
weight_kg = 1.2
rarity = 0
stack_limit = 30
interaction_text = "PRENDRE"
usable = false
tags = PackedStringArray("fuel", "craft")
